# Copy to .env and adjust as needed. All values have working defaults.

# Comma-separated list of allowed CORS origins (the frontend dev server).
EG_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Upload guard rails.
EG_MAX_FILES=10
EG_MAX_FILE_BYTES=26214400
